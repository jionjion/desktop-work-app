<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>
<BorderPane xmlns="http://javafx.com/javafx/24.0.1"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="top.jionjion.work.controller.LayoutController"
            style="-fx-background-color: #f8f9fa;">

    <!-- Top: 标题栏 -->
    <top>
        <HBox alignment="CENTER_LEFT"
              minHeight="60"
              styleClass="bg-white"
              style="-fx-padding: 0 20; -fx-border-color: #e9ecef; -fx-border-width: 0 0 1 0; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.05), 2, 0, 0, 1);">

            <!-- 左侧标题 -->
            <Label text="懒，是智慧的起点，也是推动社会进步的隐形引擎" styleClass="h6 text-primary">
                <font>
                    <Font name="Microsoft YaHei UI Bold" size="22"/>
                </font>
            </Label>

            <!-- 弹性占位 -->
            <Region HBox.hgrow="ALWAYS"/>

            <!-- 右侧按钮区 -->
            <HBox spacing="10" alignment="CENTER">
                <Label onMouseClicked="#onHelpClicked" text="❓" styleClass="text-muted" style="-fx-cursor: hand;">
                    <font>
                        <Font name="Microsoft YaHei UI" size="20"/>
                    </font>
                </Label>
            </HBox>
        </HBox>
    </top>

    <!-- Left: 工具列表 -->
    <left>
        <HBox>
            <ListView fx:id="toolList"
                      prefWidth="200"
                      minWidth="200"
                      maxWidth="200"
                      style="-fx-background-color: transparent; -fx-border-color: transparent; -fx-padding: 8 0; -fx-background-insets: 0;"
                      styleClass="bg-white"
                      VBox.vgrow="ALWAYS">
                <!-- 样式在 CSS 中控制 -->
            </ListView>
            <!-- 右侧分隔线 -->
            <Separator orientation="VERTICAL"
                       style="-fx-background-color: #e9ecef; -fx-effect: none; -fx-border-width: 0; -fx-padding: 0;"/>
        </HBox>
    </left>

    <!-- Center: 主面板 -->
    <center>
        <StackPane fx:id="mainPanel"
                   style="-fx-padding: 30; -fx-alignment: center;">
            <!-- 卡片容器 -->
            <VBox styleClass="bg-white p-4 rounded shadow-sm"
                  style="-fx-alignment: center; -fx-spacing: 20; -fx-pref-width: 400;">
                <Label text="📌 请选择左侧工具" styleClass="text-muted h5"/>
                <Label text="点击左侧菜单开始使用" styleClass="text-secondary"/>
            </VBox>
        </StackPane>
    </center>

</BorderPane>
<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Text?>

<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="top.jionjion.work.controller.PomodoroTimerController"
      spacing="20" alignment="CENTER"
      styleClass="container" style="-fx-background-color: #f8f9fa;">

    <padding>
        <Insets top="40" right="40" bottom="40" left="40"/>
    </padding>

    <!-- 标题 -->
    <HBox alignment="CENTER" spacing="10">
        <Text text="⏱" style="-fx-font-size: 32px;"/>
        <Label text="番茄钟" styleClass="h3"/>
    </HBox>

    <!-- 时间显示区 -->
    <VBox alignment="CENTER" spacing="10" styleClass="card"
          style="-fx-background-color: white; -fx-padding: 40; -fx-background-radius: 10; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 2);">
        
        <!-- 倒计时显示 -->
        <Label fx:id="timeLabel" text="25:00" 
               style="-fx-font-size: 72px; -fx-font-weight: bold; -fx-text-fill: #e74c3c;"/>
        
        <!-- 状态显示 -->
        <Label fx:id="statusLabel" text="准备开始" 
               styleClass="text-muted" style="-fx-font-size: 18px;"/>
    </VBox>

    <!-- 控制按钮区 -->
    <HBox spacing="15" alignment="CENTER">
        <Button fx:id="startBtn" text="▶ 开始" 
                styleClass="btn,btn-success" 
                onAction="#handleStart"
                prefWidth="120" prefHeight="45"
                style="-fx-font-size: 16px;"/>
        
        <Button fx:id="pauseBtn" text="⏸ 暂停" 
                styleClass="btn,btn-warning" 
                onAction="#handlePause"
                prefWidth="120" prefHeight="45"
                style="-fx-font-size: 16px;"
                disable="true"/>
        
        <Button fx:id="stopBtn" text="⏹ 停止" 
                styleClass="btn,btn-danger" 
                onAction="#handleStop"
                prefWidth="120" prefHeight="45"
                style="-fx-font-size: 16px;"
                disable="true"/>
    </HBox>

    <!-- 统计信息 -->
    <VBox spacing="10" styleClass="card"
          style="-fx-background-color: white; -fx-padding: 20; -fx-background-radius: 10; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 2);">
        
        <Label text="📊 今日统计" styleClass="h5"/>
        
        <HBox spacing="30" alignment="CENTER">
            <VBox alignment="CENTER" spacing="5">
                <Label fx:id="completedCountLabel" text="0" 
                       style="-fx-font-size: 32px; -fx-font-weight: bold; -fx-text-fill: #27ae60;"/>
                <Label text="已完成" styleClass="text-muted"/>
            </VBox>
            
            <Separator orientation="VERTICAL" prefHeight="50"/>
            
            <VBox alignment="CENTER" spacing="5">
                <Label fx:id="workTimeLabel" text="0" 
                       style="-fx-font-size: 32px; -fx-font-weight: bold; -fx-text-fill: #3498db;"/>
                <Label text="分钟" styleClass="text-muted"/>
            </VBox>
        </HBox>
    </VBox>

    <!-- 说明文字 -->
    <VBox spacing="5" alignment="CENTER">
        <Label text="🍅 专注工作 25 分钟" styleClass="text-muted"/>
        <Label text="☕ 短暂休息 5 分钟" styleClass="text-muted"/>
    </VBox>

</VBox>
